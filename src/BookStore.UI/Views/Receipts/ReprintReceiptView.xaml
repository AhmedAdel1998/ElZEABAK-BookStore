<UserControl x:Class="BookStore.UI.Views.Receipts.ReprintReceiptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BookStore.UI.Controls">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12" Orientation="Horizontal">
            <TextBox Width="180" Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}" />
            <DatePicker Width="140" Margin="8,0,0,0" SelectedDate="{Binding SaleDate}" />
            <Button Margin="8,0,0,0" Command="{Binding SearchCommand}" Content="Search" />
            <Button Margin="8,0,0,0" Command="{Binding ReprintCommand}" Content="Reprint" />
        </StackPanel>
        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedSale}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNumber}" Width="*" />
                <DataGridTextColumn Header="Date" Binding="{Binding SaleDate}" Width="180" />
                <DataGridTextColumn Header="Cashier" Binding="{Binding Cashier}" Width="160" />
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=N2}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
        <controls:NoDataView
                            Title="Receipts"
                            Message="No records found."
                            Visibility="{Binding Results.Count, Converter={StaticResource EmptyCollectionToVisibilityConverter}}" />
    </DockPanel>
</UserControl>
