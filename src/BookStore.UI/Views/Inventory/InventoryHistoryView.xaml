<UserControl x:Class="BookStore.UI.Views.Inventory.InventoryHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BookStore.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,12">
            <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Stock Ledger" />
            <controls:SecondaryButton DockPanel.Dock="Right" Command="{Binding RefreshCommand}" Content="Refresh" Icon="Refresh" />
        </DockPanel>
        <Border Grid.Row="1" Margin="0,0,0,12" Style="{StaticResource PanelBorderStyle}">
            <WrapPanel>
                <DatePicker Width="150" Margin="0,0,8,0" SelectedDate="{Binding DateFrom}" />
                <DatePicker Width="150" Margin="0,0,8,0" SelectedDate="{Binding DateTo}" />
                <ComboBox Width="180" Margin="0,0,8,0" ItemsSource="{Binding TransactionTypes}" SelectedItem="{Binding TransactionType}"><ComboBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding Converter={StaticResource EnumDisplayConverter}}" /></DataTemplate></ComboBox.ItemTemplate></ComboBox>
                <controls:PrimaryButton Command="{Binding SearchCommand}" Content="Search" Icon="Search" />
            </WrapPanel>
        </Border>
        <Grid Grid.Row="2">
            <controls:AppDataGrid HorizontalScrollBarVisibility="Auto" ItemsSource="{Binding Transactions}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Header="Date" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding ProductTitle}" Header="Product" Width="2*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding TransactionType}" Header="Transaction Type" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding QuantityChange}" Header="Quantity Change" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding QuantityBefore}" Header="Quantity Before" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding QuantityAfter}" Header="Quantity After" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding Reference}" Header="Reference" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding User}" Header="User" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding Notes}" Header="Notes" Width="2*" MinWidth="110" />
                </DataGrid.Columns>
            </controls:AppDataGrid>
            <controls:LoadingOverlay IsBusy="{Binding IsBusy}" Message="Loading stock ledger..." />
        </Grid>
        <Border Grid.Row="3" Margin="0,12,0,0" Style="{StaticResource PanelBorderStyle}">
            <controls:PaginationBar />
        </Border>
    </Grid>
</UserControl>
