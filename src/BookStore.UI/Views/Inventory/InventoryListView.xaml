<UserControl x:Class="BookStore.UI.Views.Inventory.InventoryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BookStore.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,12">
            <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Inventory" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <controls:SecondaryButton Margin="0,0,8,0" Command="{Binding HistoryCommand}" Content="Ledger" Icon="History" />
                <controls:SecondaryButton Margin="0,0,8,0" Command="{Binding RefreshCommand}" Content="Refresh" Icon="Refresh" />
                <controls:PrimaryButton Command="{Binding AdjustCommand}" Content="Adjust" Icon="Adjust" />
            </StackPanel>
        </DockPanel>
        <Border Grid.Row="1" Margin="0,0,0,12" Style="{StaticResource PanelBorderStyle}">
            <WrapPanel>
                <controls:SearchBox Width="260" Margin="0,0,12,0" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />
                <CheckBox Margin="0,6,12,0" Content="Low Stock" IsChecked="{Binding LowStockOnly}" />
                <CheckBox Margin="0,6,12,0" Content="Out of Stock" IsChecked="{Binding OutOfStockOnly}" />
            </WrapPanel>
        </Border>
        <Grid Grid.Row="2">
            <controls:AppDataGrid HorizontalScrollBarVisibility="Auto" ItemsSource="{Binding Items}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding ImagePath}" Header="Product Image" Width="1.4*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding Barcode}" Header="Barcode" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding ISBN}" Header="ISBN" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding Title}" Header="Title" Width="2*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding CategoryName}" Header="Category" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding CurrentQuantity}" Header="Current Quantity" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding MinimumStock}" Header="Minimum Stock" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding InventoryStatus}" Header="Inventory Status" Width="*" MinWidth="110" />
                    <DataGridTextColumn Binding="{Binding LastUpdated, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Header="Last Updated" Width="*" MinWidth="110" />
                </DataGrid.Columns>
            </controls:AppDataGrid>
            <controls:NoDataView
                                Title="No Products"
                                Message="No products found."
                                Visibility="{Binding Items.Count, Converter={StaticResource EmptyCollectionToVisibilityConverter}}" />
            <controls:LoadingOverlay IsBusy="{Binding IsBusy}" Message="Loading inventory..." />
        </Grid>
        <Border Grid.Row="3" Margin="0,12,0,0" Style="{StaticResource PanelBorderStyle}">
            <controls:PaginationBar />
        </Border>
    </Grid>
</UserControl>
