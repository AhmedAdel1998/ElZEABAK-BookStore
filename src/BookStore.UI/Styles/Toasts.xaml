<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Toast container style used by the shell notification ItemsControl. -->
    <Style x:Key="ToastNotificationStyle" TargetType="Border">
        <Setter Property="Width" Value="320" />
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Padding" Value="14" />
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{DynamicResource CornerRadiusMedium}" />
        <Setter Property="Effect" Value="{DynamicResource PopupShadow}" />
    </Style>
</ResourceDictionary>
